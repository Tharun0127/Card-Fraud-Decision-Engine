{% set P = r.decision.policies_test.full %}
{% set NV = r.decision.policies_test.no_velocity %}
{% set C = r.decision.comparisons %}
{% set A = r.decision.auc_vs_profit %}
{% set E = r.evaluation %}
{% set H = r.hypotheses %}
{% set S = r.sensitivity.ranking %}
{% set M = r.monitoring %}
# Card Fraud Decision Engine

![Python](https://img.shields.io/badge/python-3.12-3776AB?logo=python&logoColor=white)
![LightGBM](https://img.shields.io/badge/model-LightGBM-2a78d6)
{% if r.verification.tests_failed == 0 %}![Tests](https://img.shields.io/badge/tests-passing-1baf7a){% else %}![Tests](https://img.shields.io/badge/tests-failing-e34948){% endif +%}
![License](https://img.shields.io/badge/license-MIT-lightgrey)

**A fraud score is not a decision.** This project builds a LightGBM fraud model on
the IEEE-CIS card-not-present dataset and then does the part that usually gets
skipped: it turns the score into an **approve / review / decline policy** chosen to
maximise expected profit under a fixed manual-review capacity, and shows, on a
held-out time period, that the **profit-optimal cutoff is not the AUC-optimal
cutoff**. Every result is reported in money and customer impact, not only in AUC.

### At a glance

{% set P0 = r.decision.policies_test.full %}
- **Net benefit of the selected policy:** {{ P0.profit_three_way.net_benefit_per_1000 | usd(2) }} saved per
  {{ r.meta.per_n_transactions | int }} test transactions versus approving everything, while
  declining {{ P0.profit_three_way.good_customer_disruption_rate | pct(2) }} of legitimate customers and
  capturing {{ P0.profit_three_way.fraud_capture_rate_dollars | pct(1) }} of fraud dollars.
- **AUC-optimal vs profit-optimal:** {% if r.decision.auc_vs_profit.money_left_on_table_per_1000 > 0 %}the Youden-J cutoff leaves
  {{ r.decision.auc_vs_profit.money_left_on_table_per_1000 | usd(2) }} per {{ r.meta.per_n_transactions | int }}
  transactions on the table{%- else %}the Youden-J cutoff did not lose money against the profit-optimal cutoff on this test period
  (difference {{ r.decision.auc_vs_profit.money_left_on_table_per_1000 | usd(2) }} per {{ r.meta.per_n_transactions | int }}){% endif %}
  ({{ r.decision.comparisons.profit_single_minus_youden_single | verdict }}).
- **Against a hand-written rules baseline:** {{ r.decision.comparisons.three_way_minus_rules | ci_usd(0) }}
  more per {{ r.meta.per_n_transactions | int }} transactions.
- **Robustness to the cost assumptions:** beats the rules baseline in {{ r.sensitivity.ranking.share_model_gt_rules | pct(1) }}
  of {{ r.sensitivity.ranking.n_cells | int }} cost scenarios; profit cutoff beats Youden-J in {{ r.sensitivity.ranking.share_single_gt_youden | pct(1) }};
  full pre-registered ordering in {{ r.sensitivity.ranking.share_all_orderings | pct(1) }} (H4 **{{ r.hypotheses.H4.pass | passfail }}**).
- **Model:** test PR-AUC {{ r.evaluation.test.full.pr_auc | num(3) }}, ROC-AUC {{ r.evaluation.test.full.roc_auc | num(3) }},
  on a strictly later time period than training.
- **Verified:** {{ r.verification.tests_passed | int }} automated tests passing
  ({{ r.verification.tests_failed | int }} failing), including leakage guards and a determinism check;
  every number in this README is machine-checked against `outputs/results.json`.

The dollar figures depend on cost parameters that are **assumptions**, not
measurements, so no absolute dollar amount here is a forecast of real savings.
{% if r.hypotheses.H4.pass %}
The claim the evidence supports is about the **ranking** of policies, which held
across the pre-registered range of those assumptions.
{% else %}
Even the **ranking** of policies is only partly robust to those assumptions: the
model-driven policy beats the rules baseline in {{ r.sensitivity.ranking.share_model_gt_rules | pct(1) }} of the
pre-registered sensitivity grid, but the full pre-registered ordering holds in
{{ r.sensitivity.ranking.share_all_orderings | pct(1) }} of it, short of the {{ r.hypotheses.H4.threshold_share | pct(0) }} target (H4 failed).
{% endif %}
See [Limitations](#limitations) directly below.

## Limitations

Read these before the results. Full list with severity ratings: [FINDINGS.md](FINDINGS.md).

- **Cost parameters are invented.** Margin rate, churn penalty, review cost and
  analyst catch rate are defaults chosen in [PRE_REGISTRATION.md](PRE_REGISTRATION.md),
  not estimated from any portfolio. Absolute dollar results move by a large factor
  across the pre-registered grid (below); only the policy ranking is claimed.
- **Labels are chargebacks, not confirmed fraud.** `isFraud` marks reported
  chargebacks and propagates the label to later transactions on the same account.
  Some "fraud" is friendly fraud or disputes; some fraud is never charged back.
- **No true timestamp.** `TransactionDT` is a seconds offset from an undisclosed
  reference. It is used only to order rows and measure relative durations. Days
  and "per day" figures are in relative time.
- **Drift between training and test.** {{ M.n_significant | int }} of
  {{ M.n_features | int }} model features exceed a PSI of {{ M.psi_alert | num(2) }}
  between the training and test periods ({{ M.n_moderate | int }} more between
  {{ M.psi_warn | num(2) }} and {{ M.psi_alert | num(2) }}). Results are for one
  period of one merchant population.
- **Review queue over capacity on test.** The three-way policy was sized to
  {{ r.costs.max_reviews_per_day | num(1) }} reviews per day on validation; on test it averaged
  {{ r.decision.policies_test.full.profit_three_way.reviews_per_day | num(1) }} and exceeded capacity on
  {{ r.decision.test_days_over_capacity | int }} of {{ r.decision.test_n_days | int }} relative days. Overflow handling is not modelled.
- **One test period, one data source.** The test split covers
  {{ E.test_days | num(1) }} relative days and {{ E.test_rows | int }} transactions.
  Intervals below are bootstrap intervals over transactions within that period;
  they do not cover period-to-period variation.

## Data

{% set D = r.data %}
IEEE-CIS Fraud Detection (Vesta): {{ D.n_rows | int }} labelled card-not-present
transactions, {{ D.fraud_rate | pct(2) }} fraud, spanning {{ D.dt_span_days | num(1) }} days of relative time;
{{ D.identity_match_rate | pct(1) }} carry device and identity information. Full profile in
[DATA_DICTIONARY.md](DATA_DICTIONARY.md).

The split is strictly by time, never random:

| Split | Transactions | Fraud rate | Relative days | Used for |
|---|---:|---:|---:|---|
| Train | {{ D.by_split.train.n | int }} | {{ D.by_split.train.fraud_rate | pct(2) }} | {{ D.by_split.train.days | num(1) }} | features, tuning, model fit |
| Validation | {{ D.by_split.valid.n | int }} | {{ D.by_split.valid.fraud_rate | pct(2) }} | {{ D.by_split.valid.days | num(1) }} | choosing every threshold |
| Test | {{ D.by_split.test.n | int }} | {{ D.by_split.test.fraud_rate | pct(2) }} | {{ D.by_split.test.days | num(1) }} | reporting only |

## Features

{% set F = r.features %}
{{ F.n_features | int }} features in five families, every fitted step using training rows only:

| Family | Count | What it captures |
|---|---:|---|
| Velocity | {{ F.by_family.velocity | int }} | Per card and per account: transaction counts and amount sums over the prior hour, day and week; time since last transaction; tenure. Strictly backward-looking. |
| Entity risk | {{ F.by_family.entity_risk | int }} | Out-of-fold, smoothed historical fraud rate of product code, billing region and email domains. |
| Linkage | {{ F.by_family.linkage | int }} | Distinct cards per device, OS, browser and device fingerprint; distinct emails and addresses per card. Signals of organised fraud. |
| Raw | {{ F.by_family.raw | int }} | Amount, card attributes, counts, time deltas, match flags, identity fields (native categorical handling, no one-hot). |
| Vesta | {{ F.by_family.vesta | int }} | Pruned from the provider's engineered V-columns by NaN-group correlation filtering. |

![Feature importance](outputs/figures/feature_importance.png)

## Headline results

Population: the final {{ r.data.split_fractions.test | pct(0) }} of labelled
transactions in time order (the test split; {{ E.test_rows | int }} transactions,
{{ E.test_fraud_rate | pct(2) }} labelled fraud, {{ E.test_fraud_dollars | usd }}
of fraud by amount). All thresholds were chosen on the validation split, never on
test. Net benefit is cost saved relative to approving everything. Default cost
parameters: margin rate {{ r.costs.margin_rate | num(2) }}, churn penalty
{{ r.costs.churn_penalty | usd }}, review cost {{ r.costs.review_cost | usd }},
review catch rate {{ r.costs.review_catch_rate | num(2) }}, review capacity
{{ r.costs.max_reviews_per_day | num(1) }} cases per day
({{ r.costs.review_capacity_share | pct(0) }} of mean daily training volume).

| Policy | Net benefit per {{ r.meta.per_n_transactions | int }} txns | Net benefit, test period | Good-customer disruption rate | Fraud captured (count) | Fraud captured ($) | Reviews per day | Declined |
|---|---:|---:|---:|---:|---:|---:|---:|
| Approve everything | {{ P.approve_all.net_benefit_per_1000 | usd }} | {{ P.approve_all.net_benefit | usd }} | {{ P.approve_all.good_customer_disruption_rate | pct(2) }} | {{ P.approve_all.fraud_capture_rate_count | pct(1) }} | {{ P.approve_all.fraud_capture_rate_dollars | pct(1) }} | {{ P.approve_all.reviews_per_day | num(1) }} | {{ P.approve_all.decline_rate | pct(2) }} |
| Rules baseline (amount above training P90 and new card) | {{ P.rules.net_benefit_per_1000 | usd }} | {{ P.rules.net_benefit | usd }} | {{ P.rules.good_customer_disruption_rate | pct(2) }} | {{ P.rules.fraud_capture_rate_count | pct(1) }} | {{ P.rules.fraud_capture_rate_dollars | pct(1) }} | {{ P.rules.reviews_per_day | num(1) }} | {{ P.rules.decline_rate | pct(2) }} |
| Full model, Youden-J cutoff (AUC-optimal) | {{ P.youden_single.net_benefit_per_1000 | usd }} | {{ P.youden_single.net_benefit | usd }} | {{ P.youden_single.good_customer_disruption_rate | pct(2) }} | {{ P.youden_single.fraud_capture_rate_count | pct(1) }} | {{ P.youden_single.fraud_capture_rate_dollars | pct(1) }} | {{ P.youden_single.reviews_per_day | num(1) }} | {{ P.youden_single.decline_rate | pct(2) }} |
| Full model, profit-optimal cutoff | {{ P.profit_single.net_benefit_per_1000 | usd }} | {{ P.profit_single.net_benefit | usd }} | {{ P.profit_single.good_customer_disruption_rate | pct(2) }} | {{ P.profit_single.fraud_capture_rate_count | pct(1) }} | {{ P.profit_single.fraud_capture_rate_dollars | pct(1) }} | {{ P.profit_single.reviews_per_day | num(1) }} | {{ P.profit_single.decline_rate | pct(2) }} |
| No-velocity model, three-way policy | {{ NV.profit_three_way.net_benefit_per_1000 | usd }} | {{ NV.profit_three_way.net_benefit | usd }} | {{ NV.profit_three_way.good_customer_disruption_rate | pct(2) }} | {{ NV.profit_three_way.fraud_capture_rate_count | pct(1) }} | {{ NV.profit_three_way.fraud_capture_rate_dollars | pct(1) }} | {{ NV.profit_three_way.reviews_per_day | num(1) }} | {{ NV.profit_three_way.decline_rate | pct(2) }} |
| **Full model, three-way policy (capacity-constrained)** | **{{ P.profit_three_way.net_benefit_per_1000 | usd }}** | **{{ P.profit_three_way.net_benefit | usd }}** | **{{ P.profit_three_way.good_customer_disruption_rate | pct(2) }}** | **{{ P.profit_three_way.fraud_capture_rate_count | pct(1) }}** | **{{ P.profit_three_way.fraud_capture_rate_dollars | pct(1) }}** | **{{ P.profit_three_way.reviews_per_day | num(1) }}** | **{{ P.profit_three_way.decline_rate | pct(2) }}** |

Fraud captured counts a reviewed fraud case as caught with probability equal to
the catch rate. The three-way policy also sends
{{ P.profit_three_way.good_customer_review_rate | pct(2) }} of legitimate customers
to review, which delays but does not decline them.

Paired bootstrap comparisons on test ({{ E.n_bootstrap | int }} resamples, net
benefit in $ per {{ r.meta.per_n_transactions | int }} transactions):

| Comparison | Difference | Reading |
|---|---|---|
| Profit-optimal cutoff minus Youden-J cutoff | {{ C.profit_single_minus_youden_single | ci_usd(1) }} | {{ C.profit_single_minus_youden_single | verdict }} |
| Three-way policy minus rules baseline | {{ C.three_way_minus_rules | ci_usd(1) }} | {{ C.three_way_minus_rules | verdict }} |
| Three-way policy minus profit-optimal single cutoff | {{ C.three_way_minus_profit_single | ci_usd(1) }} | {{ C.three_way_minus_profit_single | verdict }} |
| Full model minus no-velocity model (both three-way) | {{ C.three_way_full_minus_three_way_no_velocity | ci_usd(1) }} | {{ C.three_way_full_minus_three_way_no_velocity | verdict }} |

Pre-registered hypotheses: H1 (profit cutoff beats Youden-J) **{{ H.H1.pass | passfail }}**,
H2 (three-way policy beats rules) **{{ H.H2.pass | passfail }}**,
H3 (velocity features raise PR-AUC) **{{ H.H3.pass | passfail }}**,
H4 (ranking stable in at least {{ H.H4.threshold_share | pct(0) }} of the sensitivity grid) **{{ H.H4.pass | passfail }}**.
{% if H.project_success %}
The pre-registered success rule (H1 and H2 both pass) is met.
{% else %}
The pre-registered success rule (H1 and H2 both pass) is **not** met.
{% endif %}

### Profit curve

![Profit curve](outputs/figures/profit_curve.png)

Net benefit per {{ r.meta.per_n_transactions | int }} transactions as the single
decline threshold moves, on validation (where the threshold was chosen) and test.
At the validation-selected optimum the full model saves
{{ P.profit_single.net_benefit_per_1000 | usd(2) }} per {{ r.meta.per_n_transactions | int }}
test transactions by declining {{ P.profit_single.decline_rate | pct(2) }} of them.
The test-period oracle optimum, chosen with test labels and therefore not
achievable, is {{ A.test_oracle_net_benefit_per_1000 | usd(2) }}; the gap is the
cost of choosing the threshold one period earlier.

### AUC-optimal versus profit-optimal cutoff

| | Youden-J (AUC-optimal) | Profit-optimal |
|---|---:|---:|
| Score threshold (chosen on validation) | {{ A.t_youden | num(4) }} | {{ A.t_profit | num(4) }} |
| Share of test transactions declined | {{ P.youden_single.decline_rate | pct(2) }} | {{ P.profit_single.decline_rate | pct(2) }} |
| Good-customer disruption rate | {{ P.youden_single.good_customer_disruption_rate | pct(2) }} | {{ P.profit_single.good_customer_disruption_rate | pct(2) }} |
| Legitimate customers declined (test) | {{ P.youden_single.n_false_declines | int }} | {{ P.profit_single.n_false_declines | int }} |
| Fraud captured ($) | {{ P.youden_single.fraud_capture_rate_dollars | pct(1) }} | {{ P.profit_single.fraud_capture_rate_dollars | pct(1) }} |
| Net benefit per {{ r.meta.per_n_transactions | int }} txns | {{ P.youden_single.net_benefit_per_1000 | usd(2) }} | {{ P.profit_single.net_benefit_per_1000 | usd(2) }} |

The two cutoffs differ by {{ A.threshold_gap | num(4) }} in score.
{% if A.money_left_on_table_per_1000 > 0 %}
Using the Youden-J cutoff leaves {{ A.money_left_on_table_per_1000 | usd(2) }} per
{{ r.meta.per_n_transactions | int }} transactions on the table
({{ A.money_left_on_table_total_test | usd }} over the test period,
{{ A.money_left_on_table_per_day | usd }} per relative day)
{%- if A.false_declines_avoided > 0 %}, and declines {{ A.false_declines_avoided | int }} more legitimate customers{% endif %}.
{% else %}
On this test period the Youden-J cutoff did not lose money relative to the
profit-optimal cutoff (difference {{ A.money_left_on_table_per_1000 | usd(2) }} per
{{ r.meta.per_n_transactions | int }} transactions); the profit-optimal cutoff chosen
on validation did not transfer.
{% endif %}
Youden's J weights a
missed fraud and a false decline equally in rate terms; the cost model does not,
because a missed fraud costs the full amount and a false decline costs a margin
plus a churn penalty.

![ROC with cutoffs](outputs/figures/roc_cutoffs.png)

When both cutoffs threshold the same score, their decline sets are nested, so the
swap set runs in one direction only. Its composition (amounts, card tenure,
product mix) is in [REPORT.md](REPORT.md#swap-set) and `outputs/tables/swap_set.csv`.

### Three-way policy under review capacity

![Three-way policy search](outputs/figures/three_way_policy.png)

Two thresholds split traffic into approve, review and decline. The pair was chosen
on validation as the most profitable one whose review load stays within
{{ r.costs.max_reviews_per_day | num(1) }} cases per day. Compared with the best single cutoff it adds
{{ r.decision.comparisons.three_way_minus_profit_single | ci_usd(2) }} per {{ r.meta.per_n_transactions | int }} transactions and cuts the
good-customer disruption rate from {{ r.decision.policies_test.full.profit_single.good_customer_disruption_rate | pct(2) }} to
{{ r.decision.policies_test.full.profit_three_way.good_customer_disruption_rate | pct(2) }}: borderline cases go to an analyst instead of
being declined.

### Who the Youden-J cutoff wrongly declines

{% set SW = r.decision.swap.rows %}
![Swap set](outputs/figures/swap_set.png)

The {{ SW[0].n | int }} test transactions that the Youden-J cutoff declines but the
profit-optimal cutoff approves have a fraud rate of {{ SW[0].fraud_rate | pct(2) }}, against
{{ SW[2].fraud_rate | pct(2) }} among transactions both cutoffs decline. Declining them stops little
fraud and turns away thousands of good customers; they are concentrated in product
code W. Full breakdown: [REPORT.md](REPORT.md#swap-set).

### Model metrics

Test split, full model versus no-velocity model:

| Metric | Full model | No-velocity model | Difference (paired bootstrap) |
|---|---:|---:|---|
| PR-AUC (primary) | {{ E.test.full.pr_auc | num(4) }} | {{ E.test.no_velocity.pr_auc | num(4) }} | {{ E.lift.pr_auc_full_minus_no_velocity | ci_num(4) }}; {{ E.lift.pr_auc_full_minus_no_velocity | verdict }} |
| ROC-AUC | {{ E.test.full.roc_auc | num(4) }} | {{ E.test.no_velocity.roc_auc | num(4) }} | {{ E.lift.roc_auc_full_minus_no_velocity | ci_num(4) }}; {{ E.lift.roc_auc_full_minus_no_velocity | verdict }} |
| Precision in top 1% | {{ E.test.full.precision_at_1pct | pct(1) }} | {{ E.test.no_velocity.precision_at_1pct | pct(1) }} | {{ E.lift.precision_at_1pct_full_minus_no_velocity | ci_pp(1) }} |
| Recall in top 1% | {{ E.test.full.recall_at_1pct | pct(1) }} | {{ E.test.no_velocity.recall_at_1pct | pct(1) }} | {{ E.lift.recall_at_1pct_full_minus_no_velocity | ci_pp(1) }} |
| Precision in top 5% | {{ E.test.full.precision_at_5pct | pct(1) }} | {{ E.test.no_velocity.precision_at_5pct | pct(1) }} | {{ E.lift.precision_at_5pct_full_minus_no_velocity | ci_pp(1) }} |
| Recall in top 5% | {{ E.test.full.recall_at_5pct | pct(1) }} | {{ E.test.no_velocity.recall_at_5pct | pct(1) }} | {{ E.lift.recall_at_5pct_full_minus_no_velocity | ci_pp(1) }} |

Against the rules baseline, at the rules' own flag volume
({{ E.test.rules.flag_rate | pct(2) }} of test transactions): rules precision
{{ E.test.rules.precision | pct(1) if E.test.rules.n_flagged > 0 else 'undefined (nothing flagged)' }} and recall {{ E.test.rules.recall | pct(1) }};
the full model's top transactions at the same volume reach precision
{{ E.test.full.precision_at_rules_volume | pct(1) if E.test.rules.n_flagged > 0 else 'undefined' }} and recall
{{ E.test.full.recall_at_rules_volume | pct(1) }}
(precision difference {{ E.lift.precision_at_rules_volume_full_minus_rules | ci_pp(1) }}).

### Sensitivity to the cost assumptions

Thresholds were re-selected on validation at each of {{ S.n_cells | int }} points of
the pre-registered grid. Test net benefit per {{ r.meta.per_n_transactions | int }}
transactions of the three-way policy ranges from
{{ r.sensitivity.net_benefit_per_1000_range.nb1000_profit_three_way.min | usd }} to
{{ r.sensitivity.net_benefit_per_1000_range.nb1000_profit_three_way.max | usd }} across
the grid, so absolute dollar figures should not be quoted without their
assumptions. How often each ordering of policies survives:

| Ordering on test | Share of grid cells where it holds |
|---|---:|
| Three-way policy at least as good as the single profit-optimal cutoff | {{ S.share_three_way_ge_single | pct(1) }} |
| Profit-optimal cutoff better than Youden-J | {{ S.share_single_gt_youden | pct(1) }} |
| Three-way policy better than rules baseline | {{ S.share_model_gt_rules | pct(1) }} |
| All three at once | {{ S.share_all_orderings | pct(1) }} |

![Optimal threshold across margin and churn assumptions](outputs/figures/sensitivity_threshold_margin_churn.png)

## Monitoring

{% set M = r.monitoring %}
![Feature drift](outputs/figures/psi_top_features.png)

PSI between the training and test periods flags {{ M.n_significant | int }} features above
{{ M.psi_alert | num(2) }} and {{ M.n_moderate | int }} between {{ M.psi_warn | num(2) }} and {{ M.psi_alert | num(2) }}, mostly cumulative counters that grow
with time and target-encoded features (explained in [FINDINGS.md](FINDINGS.md)). The model's
score distribution itself is stable: PSI between validation and test is
{{ M.score_psi_valid_vs_test | num(3) }}, and no weekly window exceeds {{ M.score_psi_max | num(3) }}.
Triggers, owners and retraining rules: [MONITORING_PLAN.md](MONITORING_PLAN.md).

![Score stability](outputs/figures/score_stability.png)

## How the work is verified

| Check | What it proves | Where |
|---|---|---|
| Pre-registration | Split rule, metrics, baselines, cost ranges and success criteria were fixed before any model existed | `PRE_REGISTRATION.md` |
| Time-split test | `max(train.TransactionDT) < min(validation) < min(test)`; ties never straddle a boundary | `tests/test_split.py` |
| Velocity leakage guards | On a hand-built fixture, a transaction never sees itself, same-second peers, other cards, or the future; appending future rows changes nothing | `tests/test_velocity.py` |
| Target-encoding guards | Unseen categories get the prior (not NaN, not a leaked value); a row never sees its own label; test labels are never read | `tests/test_target_encoding.py` |
| Pipeline label isolation | Flipping every validation and test label changes no feature value | `tests/test_pipeline.py` |
| Cost-function unit tests | Money is checked against hand-computed values for approve, decline, review and capacity-limited policies | `tests/test_costs.py` |
| Determinism | Features, model predictions and selected thresholds are bit-identical across runs under the fixed seed | `tests/test_pipeline.py` |
| Model archive | JSON archive (LightGBM native model string, no pickle) reloads to bit-identical predictions | `tests/test_pipeline.py` |
| Number provenance | Build fails if any number in README, REPORT, FINDINGS or MONITORING_PLAN is absent from `results.json` | `src/reporting/check_numbers.py` |

Latest run: **{{ r.verification.tests_passed | int }} passed, {{ r.verification.tests_failed | int }} failed,
{{ r.verification.tests_skipped | int }} skipped** across {{ r.verification.test_files | int }} test files.

## Architecture

```mermaid
flowchart LR
    A[Kaggle IEEE-CIS<br/>transactions + identity] --> B[Typed load and join<br/>time-ordered split]
    B --> C[Features<br/>velocity, entity risk,<br/>linkage, raw, Vesta]
    C --> D[LightGBM<br/>expanding-window CV<br/>on train only]
    D --> E[Scores on<br/>validation and test]
    E --> F[Decision layer<br/>cost model, threshold sweep,<br/>three-way policy under capacity]
    F --> G[Sensitivity<br/>across cost assumptions]
    E --> H[Monitoring<br/>PSI, score stability]
    F --> I[results.json]
    G --> I
    H --> I
    I --> J[README, REPORT,<br/>FINDINGS, figures<br/>number-checked]
```

Thresholds are chosen on the validation period and applied once to the test
period. Nothing is tuned on test.

## Run it

```bash
pip install -r requirements.txt
python -m src.run_all      # data -> features -> model -> decision layer -> monitoring -> report
python -m pytest -q
```

Stages can be run individually (`python -m src.run_all --stage decide`). The seed
is fixed in `config/pipeline.yaml` and LightGBM runs in deterministic mode, so a
rerun reproduces every number in this README.

## Layout

| Path | Contents |
|---|---|
| `PRE_REGISTRATION.md` | Split rule, metrics, baselines, cost ranges, success rule; frozen before modelling |
| `DECISIONS.md` | Design choices and the reasoning behind each |
| `DATA_DICTIONARY.md` | Column meanings, missingness, identifier cardinality, class balance by period |
| `REPORT.md` | Full results in model-risk-report form |
| `FINDINGS.md` | Severity-rated limitations and deviations from pre-registration |
| `MONITORING_PLAN.md` | What to watch in production, triggers, ownership |
| `config/` | Pipeline and cost parameters |
| `src/data`, `src/features`, `src/models` | Data, feature families, model and baselines |
| `src/decision` | Cost model, threshold sweeps, three-way policy, swap sets, sensitivity |
| `src/monitoring` | PSI and score stability |
| `src/reporting` | Figures, document rendering, number check |
| `outputs/` | `results.json`, figures (PNG), tables (CSV) |
| `artifacts/models/` | Models archived as JSON (LightGBM native model string) |
| `tests/` | Leakage guards, split, cost functions, determinism, number check |

Licence: MIT.
